<script type="text/json" id="bootstrap">
   <%= render('show.json.rabl') %>
</script>

<%= content_for :javascript do -%>
  <%= javascript_tag do %>
  	  var div = $('<div></div>');
      div.html($('#bootstrap').text());
      var data = JSON.parse(div.text());
      Syncd.initialize({collection: data});
  <% end %>
<% end %>

<%= render('show.json.rabl') %>
<%= @playlist.to_json %>